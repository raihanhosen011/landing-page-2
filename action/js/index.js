// dependency
const caollapsed = document.querySelectorAll('collapsed')
const tittle = document.querySelectorAll('collapse')

// main functionality 
caollapsed.forEach(collap => {
   console.log(collap)
})
